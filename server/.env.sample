# Backend environment variables

# MongoDB Atlas connection string (update <db_password> and cluster details).
MONGODB_URI="mongodb+srv://Admin:&lt;db_password&gt;@cluster0.vyabsir.mongodb.net/pos?retryWrites=true&amp;w=majority&amp;appName=Cluster0"

# JWT secret used to sign authentication tokens.
JWT_SECRET="change-me-in-production"

# Optional: base URL of the local/remote print agent.
# Example for local development:
# PRINT_AGENT_BASE="http://localhost:9100"
PRINT_AGENT_BASE=""
